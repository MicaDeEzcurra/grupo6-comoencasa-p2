<!DOCTYPE html>
<html lang="en">

<!-- PARTIAL HEAD -->
<%- include('./partials/head', {title: 'Vender', css: 'creacionProductoStyles.css'})%>

<!-- END OF PARTIAL HEAD -->

<body>
    <div class="body-container">
        <!-- PARTIAL HEADER -->
        <%- include('./partials/header')%>
        <!-- /PARTIAL HEADER -->
        <div class="creacionContainer">
            <h1 id="titulo">CREACION DE PRODUCTO</h1>
            <form class="uk-grid-small" action="/producto/create" method="POST" enctype="multipart/form-data">
                <div>
                    <div>
                        <input class="uk-input" type="text" name="name" placeholder="Producto"   value="<%= typeof old != 'undefined' ? old.name : ''%>">
                        <p><% if(typeof errors != 'undefined'){ %>
                            <%	if(errors.name){ %>
                            <%= errors.name.msg %>
                            <%	} %>
                            <% } %>
                        </p>
                    </div>
                    <div>
                        <label for="price"></label>
                        <input class="uk-input" type="text" name="price" placeholder="Precio"   value="<%= typeof old != 'undefined' ? old.price : ''%>">
                        <p><% if(typeof errors != 'undefined'){ %>
                            <%	if(errors.price){ %>
                            <%= errors.price.msg %>
                            <%	} %>
                            <% } %>
                        </p>
                    </div>
                    <div>
                        <label for="idCategory" class="form-label"></label>
                        <select name="idCategory" id="idCategory" class="caja">
                            <option value="0">Elegí</option>
                            <% categories.forEach(category => { %>
<<<<<<< HEAD
                            <!-- <option value="">Elegí</option> -->
                            <option value="<%= category.id %>"><%= category.name %></option>
=======
                            <option name="<%= category.name %>" value="<%= category.id %>" ><%= category.name %></option>
>>>>>>> 3c9072ce2c804b9cf20e647cf51d547f0cc031fa
                            <% }) %>
                        </select>
                        <p><% if(typeof errors != 'undefined'){ %>
                            <%	if(errors.category){ %>
                            <%= errors.category.msg %>
                            <%	} %>
                            <% } %>
                        </p>
                    </div>
                    <div>
                        <label id="seleccionarImagen" for="image" type="file"></label>
                        <input id="nuestroinput" type="file" name="img"   value="<%= typeof old != 'undefined' ? old.img : ''%>">
                        <p><% if(typeof errors != 'undefined'){ %>
                            <%	if(errors.img){ %>
                            <%= errors.img.msg %>
                            <%	} %>
                            <% } %>
                        </p>
                    </div>
                    <div class="uk-margin">
                        <label for="description"></label>
                        <textarea class="uk-textarea" name="description" rows="5" placeholder="Descripción"></textarea>
                    </div>


                    <div>
                        <button id="crear" type="submit">CREAR</button>
                    </div> <br>
                </div>
            </form>

            <!-- Naranja decoración -->
            <img src="img/naranja.jpg" alt="" class="naranja">
            <img src="/img/naranja.jpg" alt="" class="naranjax">
        </div>
        <!-- PARTIAL FOOTER -->
        <%- include('./partials/footer')%>
        <!-- END OF PARTIAL FOOTER -->
    </div>
</body>

</html>